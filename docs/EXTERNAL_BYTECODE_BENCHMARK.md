# External Bytecode Benchmark Results

Auto-generated by `scripts/external_bytecode_evaluator.py` against
`benchmarks/external_v1/bytecode/`. Every sample is runtime bytecode
fetched live from mainnet via `eth_getCode` — none of the bytes
were authored by the ChainEDR maintainers.

## Headline

- Corpus: **chainedr_external_bytecode_v1 v1.0.0**
- Samples: **4**
- CORRECT / DRIFT / REGRESS: **4 / 0 / 0**
- Total AA-\* opcode fires: **5**
- Useful fires (TP + ARCH): **5**
- **Precision on AA fires: 100.0%**

## Per-sample

| Sample | Address | Expected | Observed | Verdict | Outcome |
|---|---|---|---|---|---|
| `bb9_7702_delegator` | `0x27dbd0e7...aa61` | — | — | **N/A** | **CORRECT** |
| `entrypoint_v07` | `0x00000000...a032` | AA7702-002, AA7702-006, AA7702-015 | AA7702-002, AA7702-006, AA7702-015 | **ARCH** | **CORRECT** |
| `ithaca_account_mainnet` | `0x4337084D...f108` | AA7702-002, AA7702-006 | AA7702-002, AA7702-006 | **ARCH** | **CORRECT** |
| `permit2_deployed` | `0x00000000...8BA3` | — | — | **N/A** | **CORRECT** |

## Rationale per sample

- `bb9_7702_delegator` (N/A / CORRECT): Known 7702 delegator implementation (1,511 live delegations per Path-A hunt). Compact 789-byte runtime; the implementation has no tx.origin / extcodesize / delegatecall patterns at the opcode level.
- `entrypoint_v07` (ARCH / CORRECT): ERC-4337 EntryPoint v0.7. As infrastructure, EntryPoint legitimately uses EXTCODESIZE (target validation), DELEGATECALL (account-bound execution), and SELFDESTRUCT machinery. At the source level the AA infrastructure filter excludes these. The bytecode-mode detector cannot recover that file-level context — it surfaces the raw opcodes as architecture advisories a reviewer should manually classify.
- `ithaca_account_mainnet` (ARCH / CORRECT): Ithaca Account on mainnet. The bytecode contains code.length / extcodehash checks (likely 1271 routing) and delegatecall (executor path). At the source level (P33) we already flagged Ithaca with AA7702-012 + AA7702-013 advisories — the bytecode-level fires are consistent: real architecture surface, not exploits.
- `permit2_deployed` (N/A / CORRECT): Uniswap Permit2 deployed on mainnet. AA7702-010 fired at the SOURCE level (signature replay surface), but the opcode patterns AA7702-001 / 002 / 006 / 015 do not appear. Source-mode is the right detector for this class; bytecode-mode complementing it correctly stays silent.
